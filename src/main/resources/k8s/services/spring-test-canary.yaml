apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-test-canary
  name: spring-test-canary
spec:
  ports:
  - name: 8090-8090
    port: 8090
    protocol: TCP
    targetPort: 8090
  selector:
    app: spring-test
    env: canary
  sessionAffinity: None
  type: ClusterIP

